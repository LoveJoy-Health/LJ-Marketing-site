# Google Analytics 4 — public measurement ID (safe in client bundles).
# Scripts load only when this is set (see src/components/Analytics.tsx).
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-FTNVVFSVH7

# Optional Plausible Analytics domain
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=lovejoy.health

# Password for /investors and /investors/pitch-deck (set in the host env, not in git)
# INVESTOR_DECK_PASSWORD=
